import { queries } from "./user.sellingProduct.queries";
import { resolvers } from "./user.sellingProduct.resolver";
import { typeDefs } from "./user.sellingProduct.types";

export const UserSellingProduct = { queries, resolvers, typeDefs };
