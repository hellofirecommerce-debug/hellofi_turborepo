import { typeDefs } from "./user.buyingProduct.typedefs";
import { queries } from "./user.buyingProduct.queries";
import { resolvers } from "./user.buyingProduct.resolvers";

export const UserBuyingProduct = { typeDefs, queries, resolvers };
