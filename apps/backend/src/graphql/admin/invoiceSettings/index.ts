import { typeDefs } from "./admin.invoiceSettings.typedefs";
import { queries } from "./admin.invoiceSettings.queries";
import { mutations } from "./admin.invoiceSettings.mutations";
import { resolvers } from "./admin.invoiceSettings.resolvers";

export const AdminInvoiceSettings = { typeDefs, queries, mutations, resolvers };
