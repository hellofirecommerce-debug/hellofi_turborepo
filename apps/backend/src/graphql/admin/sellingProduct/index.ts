import { typeDefs } from "./admin.sellingProduct.typedefs";
import { queries } from "./admin.sellingProduct.queries";
import { mutations } from "./admin.sellingProduct.mutations";
import { resolvers } from "./admin.sellingProduct.resolvers";

export const AdminSellingProduct = { typeDefs, queries, mutations, resolvers };
