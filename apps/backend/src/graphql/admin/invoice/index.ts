import { mutations } from "./admin.invoice.mutations";
import { queries } from "./admin.invoice.queries";
import { resolvers } from "./admin.invoice.resolvers";
import { typeDefs } from "./admin.invoice.typedefs";

export const AdminInvoice = { typeDefs, queries, mutations, resolvers };
