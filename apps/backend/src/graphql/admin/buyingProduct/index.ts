import { typeDefs } from "./admin.buyingProduct.typedefs";
import { queries } from "./admin.buyingProduct.queries";
import { mutations } from "./admin.buyingProduct.mutations";
import { resolvers } from "./admin.buyingProduct.resolvers";

export const AdminBuyingProduct = { typeDefs, queries, mutations, resolvers };
