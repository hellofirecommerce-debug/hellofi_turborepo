import { typeDefs } from "./admin.auth.typedef";
import { mutations } from "./admin.auth.mutations";
import { queries } from "./admin.auth.queries";
import { resolvers } from "./admin.auth.resolvers";

export const AdminAuth = { typeDefs, mutations, queries, resolvers };
