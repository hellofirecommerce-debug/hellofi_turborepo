import { typeDefs } from "./admin.category.typedefs";
import { queries } from "./admin.category.queries";
import { mutations } from "./admin.category.mutations";
import { resolvers } from "./admin.category.resolvers";

export const AdminCategory = { typeDefs, queries, mutations, resolvers };
