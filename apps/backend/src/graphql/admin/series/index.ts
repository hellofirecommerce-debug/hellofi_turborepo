import { typeDefs } from "./admin.series.typedefs";
import { queries } from "./admin.series.queries";
import { mutations } from "./admin.series.mutations";
import { resolvers } from "./admin.series.resolvers";

export const AdminSeries = { typeDefs, queries, mutations, resolvers };
