import { typeDefs } from "./admin.videoReview.typedefs";
import { queries } from "./admin.videoReview.queries";
import { mutations } from "./admin.videoReview.mutations";
import { resolvers } from "./admin.videoReview.resolvers";

export const AdminVideoReviews = { typeDefs, queries, mutations, resolvers };
