import { typeDefs } from "./admin.inventory.typedefs";
import { mutations } from "./admin.inventory.mutations";
import { queries } from "./admin.inventory.queries";
import { resolvers } from "./admin.inventory.resolvers";

export const AdminInventory = { typeDefs, mutations, queries, resolvers };
