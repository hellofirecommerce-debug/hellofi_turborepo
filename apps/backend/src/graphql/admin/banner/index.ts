import { typeDefs } from "./admin.banner.typedef";
import { mutations } from "./admin.banner.mutations";
import { queries } from "./admin.banner.queries";
import { resolvers } from "./admin.banner.resolvers";

export const AdminBanner = { typeDefs, mutations, queries, resolvers };
