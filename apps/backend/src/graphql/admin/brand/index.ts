import { typeDefs } from "./admin.brand.typedefs";
import { resolvers } from "./admin.brand.resolvers";
import { mutations } from "./admin.brand.mutations";

export const AdminBrand = { typeDefs, resolvers, mutations };
