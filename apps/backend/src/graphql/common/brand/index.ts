import { typedefs } from "./common.brand.types";
import { queries } from "./common.brand.queries";
import { resolvers } from "./common.brand.resolvers";

export const CommonBrand = { typedefs, queries, resolvers };
