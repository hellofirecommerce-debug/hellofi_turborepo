import { queries } from "./common.category.queries";
import { resolvers } from "./common.category.resolvers";
import { typedefs } from "./common.category.types";

export const CommonCategory = { typedefs, resolvers, queries };
