name: Deploy Admin Frontend

on:
  push:
    branches:
      - main
    paths:
      - "apps/admin_frontend/**"
      - "packages/validations/**"
      - "packages/ui/**"
      - ".github/workflows/deploy-admin-frontend.yml"

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build validations package
        run: npm run build --workspace=packages/validations

      - name: Prepare admin frontend for Vercel
        working-directory: apps/admin_frontend
        run: |
          # Remove @repo/db dependency if it exists
          npm pkg delete dependencies."@repo/db" 2>/dev/null || true
          npm pkg set scripts.build="next build --webpack"

      - name: Deploy to Vercel
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        working-directory: apps/admin_frontend
        run: |
          npx vercel pull --yes --environment=production --token=$VERCEL_TOKEN
          npx vercel build --prod --token=$VERCEL_TOKEN
          npx vercel deploy --prod --token=$VERCEL_TOKEN
